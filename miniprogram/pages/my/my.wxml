<!--pages/my/my.wxml-->
<view 
      wx:for="{{plants}}"
      wx:key="{{item._id}}"
      id="item-{{index}}"
    >
      <image 
        src="{{item.src}}"
        mode="scaleToFill"
      ></image> 
    </view>
